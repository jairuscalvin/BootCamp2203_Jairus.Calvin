<div class="contact">
    <h1>Contact List</h1>
    <a class="btn-tambah" href="/add-contact">Tambah</a>
    <table class="table">
        <tr>
            <th>No</th>
            <th>Nama</th>
            <th>Email</th>
            <th>Action</th>
        </tr>
        <% if (contact.length < 1) { %>
            <tr>
                Data Kosong
            </tr>
        <% }else{ %>
            <% contact.forEach(kontak => { %>
            <tr>
                <td><%= index++ %></td>
                <td><%= kontak.name %></td>
                <td><%= kontak.email %></td>
                <td>
                    <div class="btn-flex">
                        <a class="btn third" href="/details/<%=kontak.name%>">detail</a>
                    </div>
                </td>
            </tr>
            <% }) %>
        <% } %> 
    </table>
</div>